#!/sbin/openrc-run

name="Redmi Note 8 (2021) USB RNDIS and SSH watchdog"
description="Continuously recover the biloba USB management link"
supervisor="supervise-daemon"
command="/usr/sbin/biloba-usb-watchdog"
respawn_delay=2

depend() {
	need localmount
	after modules
	use logger
}
