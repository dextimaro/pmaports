# USB management links are routinely checked with short TCP probes. OpenSSH
# 10.4 treats those unauthenticated probes as abuse and temporarily refuses
# the following real SSH connection from the same management host.
PerSourcePenalties no
